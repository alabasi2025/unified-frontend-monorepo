# 🗺️ نظام الخرائط SEMOP - دليل شامل

## نظرة عامة

نظام الخرائط SEMOP هو نظام خرائط أوفلاين متكامل مصمم خصيصاً لليمن، يوفر صور أقمار صناعية عالية الدقة وحديثة مع إمكانية العمل بدون اتصال بالإنترنت لضمان خصوصية البيانات. النظام مبني باستخدام Angular 20 ومكتبة Leaflet.js، ومصمم ليكون نظاماً أساسياً متكاملاً مع جميع وحدات SEMOP ERP.

## المميزات الرئيسية

### ✅ المميزات المنفذة حالياً

#### 1. **واجهة مستخدم حديثة وجذابة**
- تصميم عصري بألوان متدرجة (Gradients) جذابة
- واجهة عربية كاملة
- تصميم متجاوب يعمل على جميع الأجهزة (Desktop, Tablet, Mobile)
- رسوم متحركة سلسة لتحسين تجربة المستخدم

#### 2. **مكونات الواجهة**
- **Header**: يحتوي على عنوان النظام وأزرار الإجراءات الرئيسية
  - إضافة موقع
  - رسم مسار
  - قياس المسافة
  - إدارة الطبقات
- **Map View**: منطقة عرض الخريطة الرئيسية مع دعم Leaflet
- **Sidebar Panel**: لوحة جانبية لعرض المواقع المحفوظة
- **Floating Controls**: أزرار عائمة للتحكم السريع
  - الموقع الحالي
  - ملء الشاشة
  - الإعدادات

#### 3. **التكامل مع SEMOP**
- إضافة قائمة الخرائط في Sidebar الرئيسي
- مسار مخصص `/maps` في نظام التوجيه
- تصميم متناسق مع باقي أنظمة SEMOP

### 🔄 المميزات قيد التطوير

#### 1. **نظام الخرائط الأوفلاين**
سيتم تطوير نظام متكامل لتحميل وتخزين خرائط أوفلاين عالية الدقة:
- تحميل Tiles مسبقاً لمنطقة اليمن
- تخزين محلي باستخدام IndexedDB
- نظام Cache ذكي
- خيارات تحديث الخرائط

#### 2. **الميزات الأساسية**
- **العلامات (Markers)**: إضافة وتخصيص علامات على الخريطة
- **الملاحظات**: إضافة ملاحظات نصية للمواقع
- **الصور**: رفع وعرض صور مرتبطة بالمواقع
- **المسارات**: رسم مسارات (Polylines) على الخريطة
- **المناطق**: رسم مناطق (Polygons) محددة
- **القياسات**: قياس المسافات والمساحات
- **البحث**: البحث عن المواقع والعناوين

#### 3. **التكامل مع Backend**
- API endpoints لحفظ واسترجاع البيانات
- قاعدة بيانات للمواقع والملاحظات
- نظام مصادقة وصلاحيات
- API لرفع وإدارة الصور

#### 4. **التكامل مع وحدات SEMOP**
سيتم ربط نظام الخرائط مع جميع وحدات SEMOP:
- **الأصول (Assets)**: تتبع مواقع الأصول على الخريطة
- **المهام (Tasks)**: ربط المهام بمواقع جغرافية
- **الموظفين (Employees)**: تتبع مواقع الموظفين
- **المخزون (Inventory)**: مواقع المخازن والمستودعات
- **المشتريات (Purchases)**: مواقع الموردين
- **المبيعات (Sales)**: مواقع العملاء
- **الصيانة (Maintenance)**: مواقع أعمال الصيانة

#### 5. **جين محطات الطاقة (Power Stations Gene)**
ميزة خاصة لتتبع محطات الطاقة الشمسية:
- تتبع الألواح الشمسية
- تحديد مواقع المحطات الكهربائية
- رسم مسارات الكابلات
- لوحة تحكم متخصصة
- تقارير وإحصائيات

## البنية التقنية

### التقنيات المستخدمة

```
Frontend:
- Angular 20.3.0
- TypeScript
- Leaflet.js 1.9.4
- SCSS للتصميم

Backend (قيد التطوير):
- NestJS
- PostgreSQL/MySQL
- REST API

Server:
- Hostinger Cloud
- IP: 72.61.111.217
- Path: /var/www/semop/frontend
```

### هيكل الملفات

```
apps/platform-shell-ui/src/app/
├── features/
│   └── maps/
│       ├── maps.component.ts       # المكون الرئيسي
│       ├── maps.component.html     # القالب
│       └── maps.component.scss     # التصميم
├── layout/
│   └── main-layout.component.ts    # Sidebar مع قائمة الخرائط
├── app.routes.ts                   # مسارات التطبيق
└── ...

apps/platform-shell-ui/public/
└── assets/
    ├── marker-icon.png             # أيقونة العلامة
    ├── marker-icon-2x.png          # أيقونة عالية الدقة
    └── marker-shadow.png           # ظل العلامة
```

## التثبيت والإعداد

### المتطلبات الأساسية

```bash
Node.js >= 18.x
npm >= 9.x
Angular CLI >= 20.x
```

### خطوات التثبيت

1. **استنساخ المشروع**
```bash
git clone https://github.com/alabasi2025/unified-frontend-monorepo.git
cd unified-frontend-monorepo
```

2. **تثبيت الحزم**
```bash
npm install
```

3. **تشغيل المشروع محلياً**
```bash
npx nx serve platform-shell-ui
```

4. **بناء المشروع للإنتاج**
```bash
npx nx build platform-shell-ui --configuration=production
```

### النشر على السيرفر

```bash
# بناء المشروع
npx nx build platform-shell-ui --configuration=production

# نسخ الملفات إلى السيرفر
sshpass -p 'PASSWORD' scp -r \
  dist/apps/platform-shell-ui/browser/* \
  root@72.61.111.217:/var/www/semop/frontend/
```

## دليل الاستخدام

### الوصول إلى نظام الخرائط

1. قم بتسجيل الدخول إلى نظام SEMOP
2. من القائمة الجانبية، اختر **"🗺️ نظام الخرائط"**
3. انقر على **"🇾🇪 خريطة اليمن"**

### واجهة المستخدم

#### Header (الشريط العلوي)
يحتوي على:
- عنوان النظام والوصف
- أزرار الإجراءات الرئيسية:
  - **إضافة موقع**: لإضافة علامة جديدة
  - **رسم مسار**: لرسم خط أو مسار
  - **قياس**: لقياس المسافات
  - **الطبقات**: لإدارة طبقات الخريطة

#### Map View (منطقة الخريطة)
- منطقة عرض الخريطة الرئيسية
- يمكن التكبير والتصغير باستخدام الماوس أو أزرار +/-
- يمكن السحب للتنقل في الخريطة

#### Sidebar Panel (اللوحة الجانبية)
- عرض قائمة المواقع المحفوظة
- معلومات كل موقع (الاسم، الوصف)
- زر لعرض الموقع على الخريطة

#### Floating Controls (أزرار التحكم العائمة)
- **الموقع الحالي**: للانتقال إلى موقعك الحالي
- **ملء الشاشة**: لعرض الخريطة بملء الشاشة
- **الإعدادات**: لفتح إعدادات الخريطة

## التطوير المستقبلي

### المرحلة القادمة (Phase 3)

#### 1. تطبيق نظام الخرائط الأوفلاين

**الهدف**: توفير خرائط عالية الدقة تعمل بدون إنترنت

**الخطوات**:
1. اختيار مصدر الخرائط (ESRI World Imagery أو Mapbox Satellite)
2. تحميل Tiles لمنطقة اليمن
3. تطوير نظام تخزين محلي (IndexedDB)
4. تطبيق نظام Cache ذكي
5. إضافة خيار تحديث الخرائط

**التقنيات المقترحة**:
- Leaflet.offline plugin
- IndexedDB للتخزين
- Service Workers للـ Caching

#### 2. تطوير الميزات الأساسية

**إضافة العلامات**:
```typescript
// مثال على إضافة علامة
addMarker(lat: number, lng: number, title: string, description: string): void {
  const marker = L.marker([lat, lng])
    .addTo(this.map)
    .bindPopup(`<b>${title}</b><br>${description}`);
  
  this.markers.push(marker);
}
```

**رسم المسارات**:
```typescript
// مثال على رسم مسار
drawPolyline(points: [number, number][]): void {
  const polyline = L.polyline(points, {
    color: '#667eea',
    weight: 3
  }).addTo(this.map);
}
```

**القياسات**:
```typescript
// مثال على قياس المسافة
measureDistance(point1: [number, number], point2: [number, number]): number {
  return L.latLng(point1).distanceTo(L.latLng(point2));
}
```

### المراحل المستقبلية

#### Phase 4: Backend Integration
- تطوير API endpoints في NestJS
- إنشاء قاعدة بيانات
- ربط Frontend مع Backend

#### Phase 5: SEMOP Integration
- ربط مع وحدات SEMOP المختلفة
- تطوير واجهات مشتركة
- مزامنة البيانات

#### Phase 6: Power Stations Gene
- تطوير جين محطات الطاقة
- واجهات متخصصة
- تقارير وإحصائيات

#### Phase 7: Advanced Features
- ميزات متقدمة إضافية
- تحسينات الأداء
- تجربة مستخدم محسنة

## المساهمة

### إرشادات المساهمة

1. **Fork المشروع**
2. **إنشاء فرع جديد**
```bash
git checkout -b feature/amazing-feature
```

3. **Commit التغييرات**
```bash
git commit -m 'Add some amazing feature'
```

4. **Push إلى الفرع**
```bash
git push origin feature/amazing-feature
```

5. **فتح Pull Request**

### معايير الكود

- استخدام TypeScript بشكل صارم
- اتباع Angular Style Guide
- كتابة تعليقات واضحة بالعربية
- اختبار الكود قبل الـ Commit

## الدعم والتواصل

### الإبلاغ عن المشاكل

إذا واجهت أي مشكلة، يرجى:
1. التحقق من قائمة المشاكل الموجودة
2. إنشاء Issue جديد مع وصف مفصل
3. إرفاق Screenshots إن أمكن

### التواصل

- **GitHub**: [alabasi2025](https://github.com/alabasi2025)
- **Repository**: [unified-frontend-monorepo](https://github.com/alabasi2025/unified-frontend-monorepo)

## الترخيص

هذا المشروع مملوك لشركة SEMOP ومخصص للاستخدام الداخلي.

## الشكر والتقدير

شكراً لجميع المساهمين في تطوير نظام SEMOP Maps:
- فريق التطوير
- فريق التصميم
- فريق الاختبار
- المستخدمين الأوائل

---

**آخر تحديث**: 2025-11-21  
**الإصدار**: v1.6.0  
**الحالة**: قيد التطوير النشط 🚀
