# 🗺️ دليل نظام الخرائط الشامل - SEMOP Maps System

**الإصدار**: v1.6.0  
**تاريخ التحديث**: 2025-11-21  
**الحالة**: قيد التطوير النشط  
**المؤلف**: فريق تطوير SEMOP

---

## نظرة عامة

نظام الخرائط SEMOP هو نظام خرائط جغرافية متكامل مصمم خصيصاً لليمن، يوفر إمكانية العمل بدون اتصال بالإنترنت مع صور أقمار صناعية عالية الدقة وحديثة. النظام مبني على تقنيات حديثة (**Angular 20** و**Leaflet.js**) ومصمم ليكون نظاماً أساسياً متكاملاً مع جميع وحدات SEMOP ERP.

### الأهداف الرئيسية

النظام يهدف إلى تحقيق عدة أهداف استراتيجية تخدم احتياجات المؤسسات والشركات في اليمن. **الخصوصية والأمان** تأتي في المقدمة، حيث يعمل النظام بشكل كامل بدون اتصال بالإنترنت لضمان حماية البيانات الحساسة والمواقع الاستراتيجية. **الدقة العالية** تضمن صور أقمار صناعية بدقة تصل إلى مستوى Zoom 16 أو أعلى، مما يسمح برؤية تفاصيل دقيقة للمباني والطرق. **الحداثة** تعني استخدام صور محدثة وحديثة تعكس الوضع الحالي على الأرض. **التكامل الكامل** مع جميع وحدات SEMOP يجعل الخرائط جزءاً لا يتجزأ من عمليات الشركة اليومية. **الميزات المتقدمة** توفر إمكانيات واسعة لإضافة المواقع والملاحظات والصور ورسم المسارات والقياسات.

### الجمهور المستهدف

النظام مصمم لخدمة فئات متعددة من المستخدمين. **المستخدمون النهائيون** من موظفي الشركات الذين يستخدمون النظام في عملهم اليومي لتتبع المواقع والأصول. **مديرو المشاريع** الذين يحتاجون لتخطيط المشاريع وتتبع تقدمها جغرافياً. **المطورون** الذين يرغبون في تطوير ميزات جديدة أو تخصيص النظام. **مديرو النظام** المسؤولون عن صيانة وإدارة النظام.

---

## المخطط المعماري الشامل

### البنية العامة للنظام

نظام الخرائط SEMOP يتكون من عدة طبقات معمارية متكاملة تعمل معاً لتوفير تجربة سلسة وفعالة.

#### طبقة العرض (Presentation Layer)

هذه الطبقة تمثل الواجهة الأمامية التي يتفاعل معها المستخدم. تتضمن **مكون الخرائط الرئيسي** (`MapsComponent`) الذي يدير عرض الخريطة والتفاعل معها، **لوحة التحكم** (`ControlPanel`) التي توفر أدوات الرسم والقياس والإعدادات، **اللوحة الجانبية** (`SidebarPanel`) لعرض المواقع المحفوظة والبحث، و**نوافذ المعلومات** (`Popups & Dialogs`) لعرض تفاصيل المواقع والنماذج.

#### طبقة الخدمات (Service Layer)

طبقة الخدمات تحتوي على المنطق البرمجي الأساسي. **خدمة الخرائط** (`MapsService`) تدير تهيئة الخريطة والتفاعل مع Leaflet. **خدمة التخزين الأوفلاين** (`OfflineStorageService`) تدير تحميل وتخزين Tiles في IndexedDB. **خدمة الرسم** (`DrawingService`) توفر أدوات رسم المسارات والمناطق. **خدمة القياس** (`MeasurementService`) تحسب المسافات والمساحات. **خدمة API** (`MapsApiService`) تتواصل مع Backend لحفظ واسترجاع البيانات.

#### طبقة البيانات (Data Layer)

هذه الطبقة تدير تخزين واسترجاع البيانات. **IndexedDB** يخزن Tiles الخرائط للعمل الأوفلاين. **LocalStorage** يحفظ الإعدادات والتفضيلات. **قاعدة البيانات الخلفية** (MySQL/PostgreSQL) تخزن المواقع والملاحظات والصور. **S3 Storage** يحفظ الصور والملفات المرفوعة.

#### طبقة الخلفية (Backend Layer)

Backend مبني على **NestJS** ويوفر APIs RESTful. يتضمن **Controllers** لمعالجة الطلبات، **Services** للمنطق البرمجي، **Repositories** للتفاعل مع قاعدة البيانات، و**Middleware** للمصادقة والصلاحيات.

### مخطط تدفق البيانات

#### سيناريو 1: عرض الخريطة

عند فتح نظام الخرائط، يحدث التدفق التالي. **المستخدم** يفتح صفحة الخرائط (`/maps`). **MapsComponent** يتم تحميله ويستدعي `ngAfterViewInit()`. **MapsService** يقوم بتهيئة الخريطة باستخدام Leaflet. **OfflineTileLayer** يطلب Tiles من IndexedDB أولاً. إذا وجد Tiles محلياً، يعرضها مباشرة. إذا لم يجد، يحملها من الإنترنت ويخزنها في IndexedDB. **الخريطة** تظهر للمستخدم مع جميع العناصر.

#### سيناريو 2: إضافة موقع جديد

عند إضافة موقع جديد، يحدث التدفق التالي. **المستخدم** ينقر على زر "إضافة موقع". **Dialog** يفتح نموذج إدخال البيانات. **المستخدم** يدخل الاسم والوصف ويختار الموقع على الخريطة. **MapsComponent** يرسل البيانات إلى `MapsApiService`. **API** يحفظ الموقع في قاعدة البيانات. **Response** يعود بالموقع المحفوظ مع ID. **MapsComponent** يضيف علامة على الخريطة ويحدث القائمة الجانبية.

#### سيناريو 3: رسم مسار

عند رسم مسار، يحدث التدفق التالي. **المستخدم** ينقر على زر "رسم مسار". **DrawingService** يفعل وضع الرسم. **المستخدم** ينقر على نقاط على الخريطة. **Leaflet** يرسم خطوط بين النقاط. **المستخدم** ينهي الرسم. **MeasurementService** يحسب المسافة الإجمالية. **Dialog** يفتح لحفظ المسار مع الاسم. **API** يحفظ المسار في قاعدة البيانات.

### مخطط قاعدة البيانات

قاعدة البيانات تتكون من عدة جداول مترابطة تخزن جميع بيانات النظام.

#### جدول المواقع (locations)

هذا الجدول يخزن المواقع الأساسية. **id** (SERIAL PRIMARY KEY) معرف فريد للموقع. **name** (VARCHAR(255) NOT NULL) اسم الموقع. **description** (TEXT) وصف تفصيلي. **latitude** (DECIMAL(10,8) NOT NULL) خط العرض. **longitude** (DECIMAL(11,8) NOT NULL) خط الطول. **icon** (VARCHAR(50)) نوع الأيقونة. **color** (VARCHAR(20)) لون العلامة. **category** (VARCHAR(50)) تصنيف الموقع. **created_at** (TIMESTAMP) تاريخ الإنشاء. **updated_at** (TIMESTAMP) تاريخ التحديث. **created_by** (INTEGER REFERENCES users(id)) المستخدم المنشئ.

#### جدول الملاحظات (notes)

يخزن الملاحظات المرتبطة بالمواقع. **id** (SERIAL PRIMARY KEY) معرف فريد. **location_id** (INTEGER REFERENCES locations(id) ON DELETE CASCADE) الموقع المرتبط. **content** (TEXT NOT NULL) محتوى الملاحظة. **created_at** (TIMESTAMP) تاريخ الإنشاء. **updated_at** (TIMESTAMP) تاريخ التحديث. **created_by** (INTEGER REFERENCES users(id)) المستخدم المنشئ.

#### جدول الصور (images)

يخزن الصور المرفقة بالمواقع. **id** (SERIAL PRIMARY KEY) معرف فريد. **location_id** (INTEGER REFERENCES locations(id) ON DELETE CASCADE) الموقع المرتبط. **url** (VARCHAR(500) NOT NULL) رابط الصورة على S3. **thumbnail_url** (VARCHAR(500)) رابط الصورة المصغرة. **caption** (TEXT) وصف الصورة. **file_size** (INTEGER) حجم الملف بالبايت. **mime_type** (VARCHAR(50)) نوع الملف. **created_at** (TIMESTAMP) تاريخ الرفع. **created_by** (INTEGER REFERENCES users(id)) المستخدم الرافع.

#### جدول المسارات (paths)

يخزن المسارات المرسومة. **id** (SERIAL PRIMARY KEY) معرف فريد. **name** (VARCHAR(255)) اسم المسار. **points** (JSONB NOT NULL) نقاط المسار بصيغة JSON. **distance** (DECIMAL(10,2)) المسافة بالمتر. **color** (VARCHAR(20)) لون الخط. **width** (INTEGER) سمك الخط. **created_at** (TIMESTAMP) تاريخ الإنشاء. **created_by** (INTEGER REFERENCES users(id)) المستخدم المنشئ.

#### جدول المناطق (polygons)

يخزن المناطق المرسومة. **id** (SERIAL PRIMARY KEY) معرف فريد. **name** (VARCHAR(255)) اسم المنطقة. **points** (JSONB NOT NULL) نقاط المنطقة بصيغة JSON. **area** (DECIMAL(15,2)) المساحة بالمتر المربع. **color** (VARCHAR(20)) لون التعبئة. **opacity** (DECIMAL(3,2)) شفافية التعبئة. **created_at** (TIMESTAMP) تاريخ الإنشاء. **created_by** (INTEGER REFERENCES users(id)) المستخدم المنشئ.

### مخطط APIs

النظام يوفر مجموعة شاملة من APIs RESTful للتفاعل مع البيانات.

#### APIs المواقع

**POST /api/maps/locations** - إنشاء موقع جديد. Body يحتوي على name, description, latitude, longitude, icon, color, category. Response يعيد الموقع المحفوظ مع id.

**GET /api/maps/locations** - جلب جميع المواقع. Query parameters تشمل category, search, limit, offset. Response يعيد مصفوفة من المواقع.

**GET /api/maps/locations/:id** - جلب موقع محدد. Response يعيد تفاصيل الموقع مع الملاحظات والصور.

**PUT /api/maps/locations/:id** - تحديث موقع. Body يحتوي على الحقول المراد تحديثها. Response يعيد الموقع المحدث.

**DELETE /api/maps/locations/:id** - حذف موقع. Response يعيد status 204 No Content.

#### APIs الملاحظات

**POST /api/maps/notes** - إضافة ملاحظة. Body يحتوي على location_id, content. Response يعيد الملاحظة المحفوظة.

**GET /api/maps/notes/:locationId** - جلب ملاحظات موقع. Response يعيد مصفوفة من الملاحظات.

**PUT /api/maps/notes/:id** - تحديث ملاحظة. Body يحتوي على content. Response يعيد الملاحظة المحدثة.

**DELETE /api/maps/notes/:id** - حذف ملاحظة. Response يعيد status 204 No Content.

#### APIs الصور

**POST /api/maps/images** - رفع صورة. Body يحتوي على location_id والملف. Response يعيد معلومات الصورة مع URL.

**GET /api/maps/images/:locationId** - جلب صور موقع. Response يعيد مصفوفة من الصور.

**DELETE /api/maps/images/:id** - حذف صورة. Response يعيد status 204 No Content.

#### APIs المسارات

**POST /api/maps/paths** - حفظ مسار. Body يحتوي على name, points, distance, color, width. Response يعيد المسار المحفوظ.

**GET /api/maps/paths** - جلب جميع المسارات. Response يعيد مصفوفة من المسارات.

**DELETE /api/maps/paths/:id** - حذف مسار. Response يعيد status 204 No Content.

#### APIs المناطق

**POST /api/maps/polygons** - حفظ منطقة. Body يحتوي على name, points, area, color, opacity. Response يعيد المنطقة المحفوظة.

**GET /api/maps/polygons** - جلب جميع المناطق. Response يعيد مصفوفة من المناطق.

**DELETE /api/maps/polygons/:id** - حذف منطقة. Response يعيد status 204 No Content.

---

## البنية التقنية التفصيلية

### Frontend Architecture

#### المكونات الرئيسية (Components)

**MapsComponent** هو المكون الرئيسي الذي يدير عرض الخريطة. يقع في `/features/maps/maps.component.ts` ويحتوي على المنطق الأساسي لتهيئة الخريطة وإدارة العلامات والتفاعل مع المستخدم. المكون يستخدم Lifecycle Hooks مثل `ngOnInit()` لتهيئة الإعدادات، `ngAfterViewInit()` لتهيئة الخريطة بعد تحميل DOM، و`ngOnDestroy()` لتنظيف الموارد عند مغادرة الصفحة.

**HeaderComponent** يعرض شريط العنوان العلوي مع أزرار الإجراءات الرئيسية. يتضمن أزرار "إضافة موقع"، "رسم مسار"، "قياس"، و"الطبقات". كل زر يستدعي دالة مخصصة في MapsComponent.

**SidebarPanelComponent** يعرض لوحة جانبية على يمين الخريطة تحتوي على قائمة المواقع المحفوظة. يوفر إمكانية البحث والتصفية والانتقال السريع إلى المواقع.

**FloatingControlsComponent** يعرض أزرار تحكم عائمة في أسفل اليسار. تتضمن أزرار "الموقع الحالي"، "ملء الشاشة"، و"الإعدادات".

#### الخدمات (Services)

**MapsService** هي الخدمة الرئيسية التي تدير جميع عمليات الخريطة. توفر دوال مثل `initMap()` لتهيئة الخريطة، `addMarker()` لإضافة علامات، `removeMarker()` لحذف علامات، `flyTo()` للانتقال إلى موقع، و`setZoom()` لتغيير مستوى التكبير.

**OfflineStorageService** تدير تخزين Tiles في IndexedDB. توفر دوال مثل `saveTile()` لحفظ Tile، `getTile()` لجلب Tile، `clearOldTiles()` لحذف Tiles القديمة، و`getStorageInfo()` للحصول على معلومات المساحة المستخدمة.

**DrawingService** توفر أدوات الرسم. تتضمن دوال مثل `enableDrawing()` لتفعيل وضع الرسم، `drawPolyline()` لرسم خط، `drawPolygon()` لرسم منطقة، و`clearDrawings()` لمسح جميع الرسومات.

**MeasurementService** تحسب المسافات والمساحات. توفر دوال مثل `calculateDistance()` لحساب المسافة بين نقطتين أو مسار كامل، `calculateArea()` لحساب مساحة منطقة، و`formatDistance()` لتنسيق المسافة بوحدات مختلفة.

**MapsApiService** تتواصل مع Backend. توفر دوال مثل `createLocation()`, `getLocations()`, `updateLocation()`, `deleteLocation()` وما شابه لجميع الموارد.

#### النماذج (Models)

**Location** يمثل موقعاً على الخريطة. يحتوي على خصائص مثل id, name, description, latitude, longitude, icon, color, category, notes, images, createdAt, createdBy.

**Note** يمثل ملاحظة مرتبطة بموقع. يحتوي على id, locationId, content, createdAt, updatedAt, createdBy.

**Image** يمثل صورة مرفقة بموقع. يحتوي على id, locationId, url, thumbnailUrl, caption, fileSize, mimeType, createdAt, createdBy.

**Path** يمثل مساراً مرسوماً. يحتوي على id, name, points, distance, color, width, createdAt, createdBy.

**Polygon** يمثل منطقة مرسومة. يحتوي على id, name, points, area, color, opacity, createdAt, createdBy.

### Backend Architecture

#### Controllers

**LocationsController** يعالج طلبات المواقع. يحتوي على endpoints مثل `create()`, `findAll()`, `findOne()`, `update()`, `remove()`.

**NotesController** يعالج طلبات الملاحظات. يحتوي على endpoints مماثلة.

**ImagesController** يعالج رفع وإدارة الصور. يتضمن معالجة الملفات وضغط الصور ورفعها إلى S3.

**PathsController** يعالج طلبات المسارات.

**PolygonsController** يعالج طلبات المناطق.

#### Services

**LocationsService** يحتوي على المنطق البرمجي للمواقع. يتفاعل مع Repository ويطبق قواعد العمل.

**NotesService** يدير الملاحظات.

**ImagesService** يدير رفع الصور وضغطها وتخزينها في S3.

**PathsService** يدير المسارات.

**PolygonsService** يدير المناطق.

#### Repositories

**LocationsRepository** يتفاعل مع قاعدة البيانات لجدول locations.

**NotesRepository** يتفاعل مع جدول notes.

وهكذا لباقي الجداول.

#### Middleware & Guards

**AuthGuard** يتحقق من تسجيل دخول المستخدم.

**RolesGuard** يتحقق من صلاحيات المستخدم.

**ValidationPipe** يتحقق من صحة البيانات المدخلة.

**ErrorInterceptor** يعالج الأخطاء ويعيد رسائل واضحة.

---

## نظام الخرائط الأوفلاين

### استراتيجية التخزين

نظام الخرائط الأوفلاين يعتمد على تحميل وتخزين Tiles مسبقاً لتمكين العمل بدون إنترنت.

#### مصادر الخرائط

هناك عدة خيارات لمصادر الخرائط. **ESRI World Imagery** يوفر صور أقمار صناعية مجانية بدقة جيدة حتى Zoom 19. **Mapbox Satellite** يوفر صور عالية الدقة حتى Zoom 22 لكنه مدفوع. **Google Maps Satellite** يوفر أحدث الصور وأعلى دقة لكنه الأغلى. **OpenStreetMap** يوفر خرائط طرق مجانية لكن بدون صور أقمار صناعية.

التوصية الحالية هي استخدام **ESRI World Imagery** للبداية نظراً لكونه مجاني ويوفر دقة جيدة، ثم الانتقال إلى Mapbox أو Google Maps حسب الميزانية والاحتياجات.

#### حساب المساحة المطلوبة

لتخزين خرائط اليمن كاملة، نحتاج لحساب عدد Tiles المطلوبة. اليمن تقع بين خطوط عرض 12°N إلى 19°N وخطوط طول 42°E إلى 54°E، بمساحة تقريبية 528,000 كم².

عدد Tiles حسب مستوى Zoom:
- **Zoom 7**: ~16 tiles (نظرة عامة)
- **Zoom 10**: ~1,024 tiles (مدن رئيسية)
- **Zoom 13**: ~65,536 tiles (أحياء وشوارع)
- **Zoom 16**: ~4,194,304 tiles (مباني تفصيلية)

متوسط حجم Tile هو 15-50 KB حسب المحتوى. الحجم الإجمالي المقدر:
- **Zoom 7-13**: 1-3 GB (مناسب للتخزين المحلي)
- **Zoom 7-16**: 60-200 GB (يتطلب تخزين انتقائي)

الاستراتيجية الموصى بها هي تحميل Zoom 7-13 لكامل اليمن، وتحميل Zoom 14-16 فقط للمناطق المهمة (المدن الرئيسية، مواقع المشاريع).

#### تقنية التخزين

**IndexedDB** هي قاعدة بيانات متصفح قوية تسمح بتخزين كميات كبيرة من البيانات. نستخدمها لتخزين Tiles كـ Blobs مع مفاتيح فريدة تعتمد على URL.

هيكل التخزين:
```
Database: maps-tiles
Store: tiles
Key: tile URL (e.g., "https://server.../tile/13/3456/2345.png")
Value: {
  url: string,
  blob: Blob,
  timestamp: number,
  zoom: number,
  x: number,
  y: number
}
```

#### نظام التحميل

التحميل يتم على مراحل. **المرحلة 1** تحمل Zoom Levels المنخفضة (7-10) لكامل اليمن. **المرحلة 2** تحمل Zoom Levels المتوسطة (11-13) للمناطق المحددة. **المرحلة 3** تحمل Zoom Levels العالية (14-16) للمواقع الحرجة فقط.

التحميل يتم بشكل متوازي (5-10 tiles في وقت واحد) لتسريع العملية، مع نظام إعادة محاولة تلقائي عند الفشل، ومؤشر تقدم واضح للمستخدم.

#### نظام التحديث

Tiles المخزنة لها عمر افتراضي (مثلاً 90 يوماً). بعد انتهاء العمر، يتم تحديثها تلقائياً عند الاتصال بالإنترنت. المستخدم يمكنه أيضاً تحديث مناطق محددة يدوياً.

---

## الميزات الأساسية

### نظام العلامات (Markers)

العلامات هي نقاط على الخريطة تمثل مواقع محددة.

#### إضافة علامة

المستخدم ينقر على زر "إضافة موقع" في Header. Dialog يفتح مع نموذج يحتوي على حقول الاسم، الوصف، التصنيف، الأيقونة، واللون. المستخدم ينقر على الخريطة لتحديد الموقع، أو يدخل الإحداثيات يدوياً. بعد الحفظ، تظهر العلامة على الخريطة وتضاف إلى القائمة الجانبية.

#### تخصيص العلامات

النظام يوفر مجموعة من الأيقونات الجاهزة (مبنى، مصنع، مكتب، مستودع، محطة طاقة، إلخ). المستخدم يمكنه اختيار لون للعلامة من palette ألوان. يمكن أيضاً رفع أيقونة مخصصة.

#### تفاعل مع العلامات

النقر على علامة يفتح Popup يعرض الاسم والوصف. Popup يحتوي على أزرار "عرض التفاصيل"، "تعديل"، و"حذف". "عرض التفاصيل" يفتح Dialog كامل مع جميع المعلومات والملاحظات والصور.

### نظام الملاحظات (Notes)

الملاحظات تسمح بإضافة معلومات نصية إضافية للمواقع.

#### إضافة ملاحظة

من Dialog تفاصيل الموقع، المستخدم ينقر على "إضافة ملاحظة". محرر نص بسيط يظهر. بعد الكتابة والحفظ، تضاف الملاحظة إلى قائمة ملاحظات الموقع مع الطابع الزمني واسم المستخدم.

#### تعديل وحذف

كل ملاحظة لها أزرار "تعديل" و"حذف". فقط المستخدم المنشئ أو المدير يمكنه التعديل أو الحذف.

### نظام الصور (Images)

الصور توفر توثيق مرئي للمواقع.

#### رفع صور

من Dialog تفاصيل الموقع، المستخدم ينقر على "رفع صورة". File picker يفتح لاختيار صورة (JPG, PNG, WEBP). الصورة يتم ضغطها تلقائياً لتقليل الحجم. Thumbnail يتم إنشاؤه للعرض السريع. الصورة ترفع إلى S3 ويحفظ URL في قاعدة البيانات.

#### عرض الصور

الصور تعرض في Gallery داخل Dialog الموقع. النقر على صورة يفتحها في Lightbox بحجم كامل. يمكن التنقل بين الصور والتكبير والتصغير.

### نظام رسم المسارات (Paths)

المسارات تمثل خطوط على الخريطة مثل طرق أو مسارات حركة.

#### رسم مسار

المستخدم ينقر على زر "رسم مسار". وضع الرسم يتفعل والمؤشر يتغير. المستخدم ينقر على نقاط على الخريطة لتحديد المسار. خط يرسم تلقائياً بين النقاط. المسافة الإجمالية تحسب وتعرض. المستخدم ينقر مرتين أو يضغط Enter لإنهاء الرسم. Dialog يفتح لحفظ المسار مع اسم ولون وسمك.

#### تعديل مسار

المسارات المحفوظة يمكن تعديلها بسحب النقاط أو إضافة نقاط جديدة أو حذف نقاط.

### نظام رسم المناطق (Polygons)

المناطق تمثل مساحات محددة على الخريطة.

#### رسم منطقة

مشابه لرسم المسار، لكن يتم إغلاق الشكل تلقائياً. المساحة تحسب وتعرض بالمتر المربع أو الكيلومتر المربع. يمكن اختيار لون وشفافية التعبئة.

### نظام القياسات (Measurements)

أدوات القياس تسمح بقياس المسافات والمساحات بدقة.

#### قياس المسافة

المستخدم ينقر على زر "قياس" ثم "مسافة". ينقر على نقطتين أو أكثر على الخريطة. المسافة تحسب وتعرض بوحدات مختلفة (متر، كيلومتر، ميل). يمكن حفظ القياس كمسار.

#### قياس المساحة

مشابه لقياس المسافة، لكن يتم رسم منطقة مغلقة. المساحة تحسب وتعرض بوحدات مختلفة (متر مربع، كيلومتر مربع، هكتار).

---

## التكامل مع SEMOP

### التكامل مع نظام الأصول (Assets)

كل أصل في نظام الأصول يمكن ربطه بموقع على الخريطة. في صفحة تفاصيل الأصل، يوجد قسم "الموقع" يعرض الخريطة مع علامة الأصل. يمكن تحديث موقع الأصل بسحب العلامة. في صفحة الخرائط، يمكن تصفية العلامات لعرض الأصول فقط. النقر على علامة أصل يفتح تفاصيل الأصل.

### التكامل مع نظام المهام (Tasks)

المهام يمكن ربطها بمواقع. مثلاً، مهمة "صيانة معدة في الموقع X" ترتبط بموقع المعدة. في صفحة الخرائط، يمكن عرض جميع المهام المرتبطة بمواقع. ألوان مختلفة تمثل حالات المهام (جديدة، قيد التنفيذ، مكتملة). يمكن تخطيط مسار لزيارة عدة مواقع مهام.

### التكامل مع نظام الموظفين (Employees)

الموظفون الميدانيون يمكن تتبع مواقعهم (مع موافقتهم). في صفحة الخرائط، يمكن عرض مواقع الموظفين الحالية. يمكن تعيين مهام للموظفين بناءً على قربهم من المواقع. يمكن عرض تاريخ حركة موظف معين.

### التكامل مع نظام المخزون (Inventory)

المخازن والمستودعات تظهر على الخريطة. يمكن عرض مستوى المخزون في كل مخزن. يمكن تخطيط مسارات التوصيل بين المخازن.

### التكامل مع نظام المشتريات (Purchases)

مواقع الموردين تظهر على الخريطة. يمكن اختيار أقرب مورد لمنتج معين. يمكن تتبع شحنات المشتريات على الخريطة.

### التكامل مع نظام المبيعات (Sales)

مواقع العملاء تظهر على الخريطة. يمكن تخطيط زيارات مبيعات بناءً على المواقع. يمكن تحليل توزيع العملاء جغرافياً.

### التكامل مع نظام الصيانة (Maintenance)

مواقع أعمال الصيانة تظهر على الخريطة. يمكن تعيين فرق صيانة بناءً على القرب. يمكن تتبع تقدم أعمال الصيانة جغرافياً.

---

## جين محطات الطاقة (Power Stations Gene)

هذا جين خاص مصمم لشركات الطاقة الشمسية.

### مكونات الجين

**الألواح الشمسية** - كل لوح شمسي يمثل بعلامة على الخريطة مع معلومات القدرة والحالة. **المحطات الكهربائية** - محطات التحويل والتوزيع تظهر بأيقونات خاصة. **مسارات الكابلات** - الكابلات الكهربائية ترسم كخطوط على الخريطة مع معلومات الجهد والسعة. **لوحة التحكم** - لوحة خاصة تعرض إحصائيات مثل إجمالي القدرة المركبة، عدد الألواح، طول الكابلات، عدد المحطات.

### ميزات خاصة

**حساب القدرة الإجمالية** - جمع تلقائي لقدرات جميع الألواح في منطقة محددة. **تحليل الكفاءة** - مقارنة القدرة الفعلية مع المتوقعة بناءً على الموقع الجغرافي. **تخطيط التوسع** - أدوات لتخطيط إضافة ألواح أو محطات جديدة. **تقارير جغرافية** - تقارير مفصلة عن توزيع الطاقة جغرافياً.

---

## الأمان والصلاحيات

### نظام الصلاحيات

النظام يستخدم نموذج RBAC (Role-Based Access Control). الأدوار الأساسية هي: **Admin** يمكنه كل شيء، **Manager** يمكنه عرض وتعديل وإضافة، **User** يمكنه عرض وإضافة فقط، **Viewer** يمكنه العرض فقط.

### صلاحيات المواقع

كل موقع له مستوى خصوصية: **Public** يمكن لأي مستخدم رؤيته، **Internal** يمكن لموظفي الشركة رؤيته، **Restricted** يمكن لأعضاء فريق محدد رؤيته، **Private** يمكن للمنشئ فقط رؤيته.

### تشفير البيانات

البيانات الحساسة (الإحداثيات، الملاحظات) تشفر في قاعدة البيانات. الاتصال بين Frontend وBackend يتم عبر HTTPS. Tiles المخزنة محلياً لا تحتوي على معلومات حساسة.

---

## الأداء والتحسين

### تحسين Frontend

**Lazy Loading** - تحميل المكونات عند الحاجة فقط. **Virtual Scrolling** - عرض العناصر المرئية فقط في القوائم الطويلة. **Debouncing** - تأخير تنفيذ البحث حتى توقف المستخدم عن الكتابة. **Caching** - تخزين البيانات المحملة مؤقتاً لتجنب إعادة التحميل.

### تحسين Backend

**Database Indexing** - فهرسة الحقول المستخدمة في البحث (latitude, longitude, category). **Query Optimization** - استخدام JOINs فعالة وتجنب N+1 queries. **Caching** - استخدام Redis لتخزين البيانات المتكررة. **Pagination** - تحميل البيانات على دفعات بدلاً من كلها مرة واحدة.

### تحسين الخرائط

**Tile Caching** - تخزين Tiles محلياً لتجنب إعادة التحميل. **Progressive Loading** - تحميل Zoom Levels المنخفضة أولاً. **Clustering** - تجميع العلامات القريبة عند Zoom منخفض. **Simplification** - تبسيط المسارات والمناطق عند Zoom منخفض.

---

## الاختبار

### اختبارات الوحدة (Unit Tests)

كل Service وComponent يجب أن يكون له اختبارات وحدة. نستخدم **Jest** للاختبارات. مثال:

```typescript
describe('MapsService', () => {
  it('should calculate distance correctly', () => {
    const service = new MapsService();
    const distance = service.calculateDistance(
      [15.5527, 48.5164],
      [15.6000, 48.6000]
    );
    expect(distance).toBeCloseTo(10000, -2);
  });
});
```

### اختبارات التكامل (Integration Tests)

اختبار التفاعل بين المكونات والخدمات. مثال: اختبار إضافة موقع من البداية للنهاية.

### اختبارات الأداء (Performance Tests)

قياس وقت تحميل الخريطة، وقت إضافة 1000 علامة، وقت البحث في 10000 موقع.

### اختبارات المستخدم (User Acceptance Tests)

اختبار مع مستخدمين حقيقيين لجمع الملاحظات وتحسين التجربة.

---

## النشر والصيانة

### بيئة التطوير

**Local Development** - كل مطور لديه نسخة محلية من النظام. **Development Server** - سيرفر مشترك للاختبار الأولي.

### بيئة الاختبار

**Staging Server** - نسخة مطابقة للإنتاج للاختبار النهائي. **Testing Database** - قاعدة بيانات منفصلة مع بيانات تجريبية.

### بيئة الإنتاج

**Production Server** - Hostinger (72.61.111.217). **Production Database** - قاعدة بيانات آمنة مع نسخ احتياطية يومية. **CDN** - لتسريع تحميل الملفات الثابتة.

### عملية النشر

1. **Build** - بناء المشروع للإنتاج
2. **Test** - تشغيل جميع الاختبارات
3. **Backup** - نسخ احتياطي لقاعدة البيانات والملفات
4. **Deploy** - نسخ الملفات إلى السيرفر
5. **Migrate** - تشغيل migrations لقاعدة البيانات
6. **Verify** - التحقق من عمل النظام
7. **Monitor** - مراقبة الأداء والأخطاء

### الصيانة

**مراقبة الأداء** - استخدام أدوات مثل New Relic أو Datadog. **معالجة الأخطاء** - نظام تتبع أخطاء مثل Sentry. **النسخ الاحتياطية** - نسخ احتياطية يومية تلقائية. **التحديثات** - تحديث المكتبات والأمان بانتظام.

---

## الملحقات

### مسرد المصطلحات

**Tile** - قطعة صغيرة من الخريطة (عادة 256×256 بكسل). **Zoom Level** - مستوى التكبير (0 = العالم كله، 20 = تفاصيل دقيقة جداً). **Marker** - علامة على الخريطة تمثل موقعاً. **Polyline** - خط مرسوم على الخريطة. **Polygon** - منطقة مغلقة مرسومة على الخريطة. **IndexedDB** - قاعدة بيانات متصفح لتخزين بيانات كبيرة. **Leaflet** - مكتبة JavaScript لعرض الخرائط. **ESRI** - شركة توفر خدمات خرائط وGIS.

### الموارد الإضافية

**Leaflet Documentation** - https://leafletjs.com/reference.html  
**ESRI World Imagery** - https://server.arcgisonline.com/  
**IndexedDB API** - https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API  
**Angular Documentation** - https://angular.dev/  
**NestJS Documentation** - https://docs.nestjs.com/

### جهات الاتصال

**فريق التطوير** - dev@semop.com  
**الدعم الفني** - support@semop.com  
**GitHub** - https://github.com/alabasi2025

---

## الخلاصة

نظام الخرائط SEMOP هو نظام شامل ومتطور يوفر إمكانيات قوية لإدارة المواقع والأصول جغرافياً. النظام مصمم ليكون آمناً وفعالاً وسهل الاستخدام، مع إمكانية التوسع والتخصيص حسب احتياجات كل شركة.

المرحلة الأولى (البنية الأساسية والواجهة) مكتملة بنجاح. المراحل القادمة ستضيف الخرائط الأوفلاين والميزات المتقدمة والتكامل الكامل مع جميع وحدات SEMOP.

نحن متحمسون لرؤية هذا النظام ينمو ويتطور ليصبح أداة أساسية لجميع مستخدمي SEMOP.

---

**إعداد**: فريق تطوير SEMOP  
**التاريخ**: 2025-11-21  
**الإصدار**: 1.0  
**الحالة**: وثيقة حية تحدث مع تطور النظام

---

*"الخرائط ليست فقط لمعرفة أين نحن، بل لتخطيط إلى أين نذهب"*
